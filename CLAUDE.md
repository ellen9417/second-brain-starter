# Second Brain — 나만의 지식/노하우 위키

<오너 이름>의 업무·학습에서 쌓이는 지식을 축적하는 개인 위키다.
Claude가 자료를 읽고 정리해 위키로 만들고, 오너는 필요할 때 질문한다.
(Karpathy의 LLM Wiki 아이디어 기반: https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f)

## 0. 오너 맥락 (모든 정리의 기준) — ★먼저 채울 것

> 자료를 정리할 때 이 맥락을 렌즈로 삼아 "그래서 나에게 무슨 의미인가"를 먼저 뽑아낸다.

| 항목 | 내용 |
|------|------|
| 역할 | (예: AI 서비스 기획자 / 백엔드 개발자 / 데이터 분석가) |
| 핵심 미션 | (지금 가장 중요한 과제 한 줄) |
| 확장 영역 | (미션 주변에서 넓혀가는 영역들) |
| 도메인 | (내 업무 도메인 — 정리 시 용어·우선순위 판단 기준) |
| 커리어 방향 | (성장하려는 방향 — 커리어 렌즈의 기준) |

### 방향성 렌즈 — 이중 관점 (필수 규칙)

**모든 위키 페이지의 맨 앞에 `>` callout 블록으로 핵심 takeaway를 먼저 넣는다.**
구체적 내용보다 "그래서 나에게 무슨 의미인가"가 첫 문단에서 바로 보여야 한다.
takeaway는 **두 렌즈**로 분리해서 적는다 (렌즈 이름은 §0에 맞게 조정 가능):

```markdown
> **🎯 실무 적용:** 이 내용이 내 현재 미션·과제에 어떻게 기여하는가, 지금 무엇을 적용할 수 있는가.
> **📈 커리어 자산:** 이 지식이 내 역량·포트폴리오에 어떤 의미인가, 어떤 무기가 되는가.
```

둘 중 한쪽이 해당 없으면 "해당 없음"이라 쓰지 말고 약하게라도 연결을 시도하되, 정말 무관하면 그 줄을 생략한다.

## 0.5 온톨로지 레이어 (Semantic·Kinetic·Dynamic)

이 위키는 단순 문서 모음이 아니라 **온톨로지**다. 스키마(헌법)는 `wiki/_ontology.md`, 개체 레지스트리는 `wiki/_entities.md`.

- **모든 콘텐츠 페이지는 frontmatter에 `type:` 한 개를 갖는다** (`wiki/_ontology.md` §1의 타입만 사용).
- **의미 있는 연결은 타입드 관계로** 표기한다 (`relations:` 또는 inline `impacts:: [[…]]`). 중앙 집계 = `_entities.md`.
- 신규 type/relation이 필요하면 `_ontology.md`에 **먼저 등록**한 뒤 사용.
- `Project`/`Case`/`Record`는 가능하면 **Kinetic 바인딩**(`src_jira`·`src_confluence`·`src_calendar` 등)을 선언해 현재 상태를 pull할 수 있게 한다.
- **SoT 우선순위:** "내 할 일·진행 상황"의 소스 신뢰 순서를 `_ontology.md` §4에 정의하고 따른다. (기본 예시: 내가 편집한 문서 > 내가 저장한 메시지 > 캘린더 > 티켓 시스템 — 티켓은 남이 시킨 일 뷰라 내 의도와 다를 수 있어 단독 신뢰 금지)
- **Kinetic 자동 Pull (답변 전용 모드):** `src_*` 바인딩이 있는 페이지의 주제로 질문받으면, 바인딩된 소스를 **실시간 조회해 답변에 반영**한다. 단 —
  - 🟢 **답변 구성에만 사용.** pull한 최신 데이터로 **위키 페이지를 자동 수정하지 않는다.**
  - 위키 본문 갱신은 종전대로 "정리해줘"(ingest) 명시 요청이 있을 때만, 승인 후 수행한다.
  - 답변 시 정적 본문과 pull한 실시간 값이 다르면, **실시간 값을 우선**하되 "위키 기록 X / 현재 Y"로 구분해 보여준다.
  - pull 실패(소스 없음·권한 등) 시 추측하지 말고 "소스 조회 실패"를 밝힌다.
- 의사결정은 **버전관리**한다(`_ontology.md` §7): AI 제안=branch → 오너 승인=PR → 반영=merge → `log.md`에 기록.
- 건강검진(운영3)은 **온톨로지 정합성 검사**(`_ontology.md` §8)를 포함한다.

## 1. 폴더 구조

```
second-brain/
├── CLAUDE.md          # 이 파일 — 위키 운영 규칙
├── raw/               # 원본 자료 (오너가 넣고, Claude는 읽기만)
└── wiki/              # 정리된 지식 (Claude가 작성·관리)
    ├── index.md       # 전체 목차 — 자료 넣을 때마다 갱신
    ├── log.md         # 작업 이력
    ├── overview.md    # 위키 전체 종합 개요
    ├── _ontology.md   # 온톨로지 스키마 (헌법)
    ├── _entities.md   # 개체 레지스트리
    └── *.md           # 개별 지식 페이지 (하위 폴더 없이 플랫)
```

- **raw/ 와 wiki/ 모두 하위 폴더 없이 플랫하게 관리한다.**
- **분류는 폴더가 아니라 `wiki/index.md`의 카테고리로 한다.**
- 페이지 파일명은 한글 kebab 형태 (예: `llm-회의록-정리-노하우.md`). Obsidian `[[위키링크]]` 호환.

## 2. 운영 방법

### 📥 운영 1 — 자료 넣기 (Ingest)

트리거: "이거 정리해줘", "위키에 넣어줘", "raw에 넣었어", 또는 raw/에 새 파일이 보일 때.

1. **읽기** — raw/의 해당 자료를 읽는다. (raw는 절대 수정하지 않는다)
2. **렌즈 적용** — §0 맥락으로 두 takeaway를 먼저 뽑는다.
3. **페이지 작성/갱신** — `wiki/`에 마크다운 페이지를 만든다.
   - 맨 앞: `>` callout 이중 렌즈 takeaway (필수)
   - 그 아래: 구체적 내용 (핵심 요약 → 상세 → 원본 출처 링크)
   - frontmatter: `title`, `type`, `category`, `tags`, `source`(raw 경로), `created`, `updated`
   - **기존 페이지와 주제가 겹치면 새로 만들지 말고 기존 페이지를 갱신**한다.
4. **교차참조 (필수)** — 관련된 다른 페이지를 `[[페이지명]]`으로 적극 연결한다.
   아직 없는 페이지라도 `[[미래페이지]]`로 링크를 걸어 둔다 (만들 거리 표시).
5. **index.md 갱신** — 적절한 카테고리에 새 페이지 한 줄 추가.
6. **log.md 갱신** — `[날짜] ingest | 무엇을 했는지` 한 줄.

### ❓ 운영 2 — 질문하기 (Ask)

트리거: 오너가 위키 내용에 대해 질문할 때.

1. `wiki/index.md`에서 관련 페이지를 찾고, 해당 페이지(들)과 `[[링크]]`로 연결된 페이지를 읽는다.
2. 위키에 근거가 있으면 **출처 페이지를 명시**해 답한다 (`→ [[페이지명]]`).
3. 위키에 없으면 "위키에 없음"을 분명히 밝히고, raw/에서 찾거나 일반 지식으로 답하되 구분한다.
4. 답하다가 위키에 빠진 게 보이면 보충 제안 → 승인 시 운영 1로 페이지 갱신.

### 🩺 운영 3 — 건강검진 (Health Check)

트리거: "건강검진", "위키 점검", "정리 상태 봐줘", 또는 주기적으로.

1. **고아 페이지** — 어디서도 링크되지 않은 페이지 탐지.
2. **깨진 링크** — `[[링크]]` 대상 페이지가 없는 경우 (→ 만들지/지울지 제안).
3. **중복·분할** — 같은 주제가 여러 페이지에 흩어졌는지, 너무 큰 페이지를 쪼갤지.
4. **신선도** — 오래되어 사실과 어긋날 수 있는 페이지 (특히 진행 과제·수치).
5. **index 정합성** — index.md에 빠지거나 잘못 분류된 페이지.
6. **미반영 raw** — raw/에 있는데 아직 위키로 정리되지 않은 자료 목록.
7. **takeaway 누락** — callout 이중 렌즈가 없거나 형식이 어긋난 페이지.
8. **온톨로지 정합성** — `_ontology.md` §8 (type 누락, 미등록 관계, 끊긴 바인딩, 방치된 branch, 규모 체크).

결과는 진단 리포트로 보고하고, 수정은 승인받아 진행한다. 점검 후 log.md에 기록한다.

### ☀️ 운영 4 — Daily Standup (온디맨드)

트리거: 오너가 **"standup"**(또는 "오늘 할 일", "아침 브리핑") 이라고 할 때. *(자동 실행 아님 — 부를 때만)*
SoT 우선순위(§0.5)로 소스를 실시간 pull해 **답변에만** 쓴다(위키 미수정).

1. **📅 오늘 일정** — 캘린더(오늘) 회의 목록. 없으면 "집중 작업일".
2. **🔴 즉시 액션** — 내가 저장해둔 메시지(액션 큐) 최근 3일분 → 액션 항목.
3. **🟡 내가 벌여둔 미결** — 내가 최근 편집한 문서에서 본인이 연 미결/후속.
4. **🟢 보조 — 전일 멘션** — 전일 나에게 온 멘션을 발신자별로 요약 → 할 일로 변환.
5. **각 항목에 원본 소스 링크를 첨부한다** — 클릭 한 번에 원본으로 가게. 관련 위키 페이지는 `[[링크]]`로 함께.
6. 출처(소스)와 pull 시점을 밝힌다. 빈 섹션은 "없음"으로 솔직히.

## 3. 페이지 작성 규칙

- **언어:** 한국어 (오너 선호에 맞게 조정).
- **callout 이중 렌즈:** 맨 앞 필수 (§0 규칙).
- **교차참조 적극 활용:** 페이지를 추가할 때마다 관련 페이지를 `[[링크]]`로 엮는다. 고립 페이지를 만들지 않는다.
- **출처 명시:** 페이지 하단에 원본 raw 경로 / URL 등 출처를 남긴다.
- **사실과 추론 구분:** raw에 근거한 사실과 Claude의 해석·추론을 구분해 표기한다.
- **갱신 우선:** 같은 주제는 새 페이지보다 기존 페이지 갱신. updated 날짜 갱신.

### 페이지 템플릿

```markdown
---
title: 페이지 제목
type: (Project|Component|Policy|Domain|Method|Case|Record|Career|Personal|Person 중 1개)
category: 카테고리명
tags: [태그1, 태그2]
source: raw/원본파일.md
created: YYYY-MM-DD
updated: YYYY-MM-DD
relations:
  part_of: "[[상위페이지]]"
---

# 페이지 제목

> **🎯 실무 적용:** ...
> **📈 커리어 자산:** ...

## 핵심 요약
- ...

## 상세 내용
...

## 관련 페이지
- [[관련페이지1]]
- [[관련페이지2]]

---
*출처: 원본 경로 / URL*
```
